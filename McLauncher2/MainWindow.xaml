<Window x:Class="McLauncher2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="McLauncher2" ResizeMode="CanMinimize" Width="500"        
        Closing="MainWindow_Closing" WindowStyle="None" FontFamily="Segoe UI" 
        SizeToContent="WidthAndHeight" AllowDrop="True" Loaded="Window_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="Border" Color="#FFA7A7A7"/>
        <Style TargetType="Button">        
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="CornflowerBlue" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="CornflowerBlue" />
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="Orange"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                        <Grid>                            
                            <Rectangle Fill="{TemplateBinding Background}" Stroke="{StaticResource Border}" />
                            <ContentPresenter Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="FontSize" Value="14" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Style.Triggers>                            
                            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                <Setter Property="Background" Value="White" />
                            </Trigger>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="WhiteSmoke" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <StackPanel>
        <Border Padding="0" Background="Purple">
            <Label Content="McLauncher2" 
               HorizontalAlignment="Left"
               Foreground="White"
               FontFamily="Segoe UI" FontSize="15"
               />
        </Border>
        <Border Padding="0" BorderBrush="Purple" BorderThickness="1">
            <Grid Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="180"/>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                </Grid.RowDefinitions>
                <Button Name="Button_TargetFolder" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                Content="Choose Target Folder" 
                Click="Button_TargetFolder_Click">
                </Button>
                <Label Name="Label_TargetFolder" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4"
               Content="No Setting" 
               FontFamily="Segoe UI" 
               FontSize="13"               
               VerticalAlignment="Center"/>
                <ListBox Name="ListBox_TargetList" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                 Margin="5" 
                 ItemsSource="{Binding}"
                 SelectionMode="Single"
                 SelectionChanged="ListBox_TargetList_SelectionChanged"
                 />
                <ListBox Name="ListBox_Target" Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2"
                 Margin="5" 
                 ItemsSource="{Binding}"
                 SelectionMode="Single"
                 MouseDoubleClick="ListBox_Target_MouseDoubleClick"/>
                <Button Name="Button_Run" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                Content="RUN" 
                Click="Button_Run_Click"/>
                <Button Name="Button_ConfigEditor" Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2"
                Content="McConfigEditor" 
                Click="Button_ConfigEditor_Click" />
                <Button Name="Button_Setting" Grid.Row="4" Grid.Column="0"
                Content="SETTING" 
                Click="Button_Setting_Click" />
                <Button Name="Button_EditBat" Grid.Row="4" Grid.Column="1"
                Content="CUSTOMIZE" 
                Click="Button_EditBat_Click"/>
                <Button Name="Button_Memo" Grid.Row="4" Grid.Column="2"
                Content="MEMO" 
                Click="Button_Memo_Click"/>
                <Button Name="Button_Exit" Grid.Row="4" Grid.Column="3"
                Content="EXIT" 
                Click="Button_Exit_Click"/>
            </Grid>
        </Border>
    </StackPanel>
</Window>
